% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fect.R
\name{didgpu_fect}
\alias{didgpu_fect}
\title{Counterfactual-prediction DiD estimators (fect family)}
\usage{
didgpu_fect(
  df,
  outcome,
  group,
  time,
  treatment,
  method = c("fe", "ife", "mc"),
  effects = 1L,
  r = 2L,
  lambda = NULL,
  tol = 1e-05,
  max_iter = 500L,
  bootstrap_reps = 100L,
  seed = 1L,
  checkpoint_dir = NULL,
  backend = "auto",
  n_workers = 1L,
  verbose = TRUE
)
}
\arguments{
\item{df}{A data.frame (or data.table) panel. Must contain the
columns named by \code{outcome}, \code{group}, \code{time}, \code{treatment}.}

\item{outcome, group, time, treatment}{Character. Column names.}

\item{method}{One of \code{"fe"}, \code{"ife"}, \code{"mc"}. Default \code{"fe"}.}

\item{effects}{Integer. Number of post-treatment event-times to
estimate (e = 1..effects). Must be >= 1.}

\item{r}{Integer. For method \code{"ife"}, the number of latent factors.
Default \code{2L}. Ignored for \code{"fe"} and \code{"mc"}.}

\item{lambda}{Numeric or NULL. For method \code{"mc"}, the nuclear-norm
penalty parameter. If NULL (default), chosen by cross-validation
over a default grid. Ignored for \code{"fe"} and \code{"ife"}.}

\item{tol}{Numeric. Convergence tolerance for the iterative methods
(\code{"fe"}, \code{"ife"}, \code{"mc"}). Default \code{1e-5}.}

\item{max_iter}{Integer. Maximum iterations for the iterative
methods. Default \code{500L}.}

\item{bootstrap_reps}{Integer. Number of bootstrap iterations.}

\item{seed}{Integer. RNG seed for bootstrap iter 1 onward (iter 0 is
the deterministic point estimate). Per-iter seed is \code{seed + iter}.}

\item{checkpoint_dir}{Character or NULL. If non-NULL, every cell is
saved here and the run is resumable. If NULL, all work is in-memory
and is lost on crash.}

\item{backend}{One of \code{"auto"}, \code{"reference"}, \code{"r"}, \code{"cpu"}, \code{"cuda"}.
See \code{didgpu_backend_info()}.}

\item{n_workers}{Integer >= 1. Number of parallel worker processes
for the bootstrap loop. 1 = sequential (default). With n_workers > 1,
uses \code{parallel::makeCluster()} to distribute bootstrap iters across
cores. Each cell is saved to disk atomically so resume still works.
The point estimate (cell 0) always runs sequentially first.}

\item{verbose}{Logical. Print one line per completed cell.}
}
\value{
An object of class \code{didgpu_fect_result} — same general shape
as \code{didgpu_result} (effects table, placebo table, ATE, S3 methods
work the same way).
}
\description{
Fits one of three counterfactual-prediction estimators (Liu, Wang, Xu
2024) on a panel and returns an estimate of the average treatment
effect on the treated (ATT) along with placebo-style robustness
diagnostics. The three methods differ in how they model the
counterfactual outcome Y(0):
}
\details{
\itemize{
\item \code{"fe"}  — two-way fixed effects (unit + time FE), no factor
loadings. Fast; requires the strict parallel-trends
assumption.
\item \code{"ife"} — interactive fixed effects (Bai 2009): unit FE +
time FE + \code{r} latent factors \verb{lambda_i' F_t}. Relaxes
parallel trends to allow unit-specific time-varying
confounders.
\item \code{"mc"}  — matrix completion (Athey et al. 2021): low-rank
recovery of the control-only outcome matrix via
nuclear-norm soft-thresholding. No need to choose \code{r}
in advance.
}

All three share the per-cell checkpoint + resume + parallel
bootstrap infrastructure of \code{\link[=didgpu]{didgpu()}}.

\strong{Status: SCAFFOLDED.} The R-side public API is stable; the actual
estimation logic is not yet implemented. Each method currently
raises a \code{NotImplementedError}. The CUDA + Rcpp+Eigen backends for
these will land in subsequent releases.
}
\examples{
\dontrun{
# NOT YET IMPLEMENTED -- the example shows the planned interface only.
p <- didgpu_simulate_panel(n_units = 100L, n_periods = 20L, seed = 17L)
fit <- didgpu_fect(p, outcome = "Y", group = "unit",
                    time = "period", treatment = "D",
                    method = "ife", r = 2L,
                    bootstrap_reps = 100L,
                    checkpoint_dir = "checkpoints/fect_run1")
print(fit)
plot(fit)
}

}
\references{
\itemize{
\item Liu, L., Wang, Y., and Xu, Y. (2024). "A practical guide to
counterfactual estimators for causal inference with time-series
cross-sectional data." \emph{American Journal of Political Science}.
\item Bai, J. (2009). "Panel data models with interactive fixed
effects." \emph{Econometrica} 77 (4): 1229-1279.
\item Athey, S., Bayati, M., Doudchenko, N., Imbens, G., and Khosravi, K.
(2021). "Matrix completion methods for causal panel data models."
\emph{JASA} 116 (536): 1716-1730.
}
}
