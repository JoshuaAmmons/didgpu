% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/equivalence.R
\name{didgpu_equivalence}
\alias{didgpu_equivalence}
\title{Pre-trends equivalence (TOST) test for a didgpu event study}
\usage{
didgpu_equivalence(x, delta, alpha = 0.05)
}
\arguments{
\item{x}{A \code{didgpu_result} from \code{\link[=didgpu]{didgpu()}} run with \code{placebo > 0} and
\code{bootstrap_reps > 0} (equivalence testing needs standard errors).}

\item{delta}{Positive numeric equivalence margin on the outcome scale --
the largest pre-trend you would consider economically negligible.}

\item{alpha}{One-sided significance level (default 0.05).}
}
\value{
A \code{didgpu_equivalence} object (a data.frame with one row per
placebo horizon: \code{event_time}, \code{estimate}, \code{std.error},
\code{equivalence_p}, \code{passes_at_delta}) carrying attributes \code{delta},
\code{alpha}, \code{joint_pass} (TRUE iff every horizon passes; NA if any SE is
missing) and \code{breakdown_delta} (the smallest margin at which the joint
equivalence would hold at level \code{alpha}).
}
\description{
Runs a two-one-sided-tests (TOST) equivalence test on the placebo
(pre-treatment) estimates of a \code{\link[=didgpu]{didgpu()}} fit. For a user-supplied
margin \code{delta}, each placebo horizon tests
\eqn{H_0: |\theta| \ge \delta} against \eqn{H_1: |\theta| < \delta};
rejecting \eqn{H_0} is positive evidence the pre-trend is within
\eqn{\pm}\code{delta}. The joint claim "all placebos lie within \code{delta}"
follows by the intersection-union principle: it holds iff every horizon
individually rejects.
}
\details{
Unlike a conventional placebo test (where a \emph{large} p-value is the
hoped-for result but only weakly informative), here a \emph{small}
\code{equivalence_p} is the hoped-for result and is a genuine rejection.
}
\examples{
\donttest{
p <- didgpu_simulate_panel(n_units = 80L, n_periods = 12L,
                           tau_profile = c(0.5, 1.0), seed = 7L)
p$D <- as.integer(p$D >= 0.5)
fit <- didgpu(p, "Y", "unit", "period", "D", effects = 3L, placebo = 3L,
              bootstrap_reps = 200L, verbose = FALSE)
didgpu_equivalence(fit, delta = 0.5)
}
}
\seealso{
\code{\link[=didgpu]{didgpu()}}, \code{\link[=didgpu_fect_equivalence]{didgpu_fect_equivalence()}} for the fect-family
analogue, \code{\link[=didgpu_honest_did]{didgpu_honest_did()}} for HonestDiD sensitivity bounds.
}
