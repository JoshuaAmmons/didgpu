% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{didgpu_simulate_panel_bidir}
\alias{didgpu_simulate_panel_bidir}
\title{Generate a panel with BOTH switcher-in and switcher-out units}
\usage{
didgpu_simulate_panel_bidir(
  n_units = 100L,
  n_periods = 20L,
  frac_treated = 0.6,
  frac_in = 0.5,
  min_treat_period = NULL,
  max_treat_period = NULL,
  tau_in = c(0.5, 1, 1.2),
  tau_out = c(-0.5, -0.8, -1),
  sigma = 0.4,
  unit_fe_sd = 1,
  time_fe_sd = 0.3,
  seed = 1L
)
}
\arguments{
\item{n_units}{integer. Number of units.}

\item{n_periods}{integer. Number of time periods.}

\item{frac_treated}{numeric between 0 and 1. Share that ever switch.}

\item{frac_in}{numeric between 0 and 1. Of switchers, share going
in (rest go out). 0 = all out-switchers, 1 = all in-switchers.}

\item{min_treat_period, max_treat_period}{Treatment window.}

\item{tau_in, tau_out}{numeric vectors. Event-time profiles for
in-switchers and out-switchers respectively.}

\item{sigma, unit_fe_sd, time_fe_sd, seed}{As in \code{didgpu_simulate_panel}.}
}
\value{
Data.frame with (unit, period, D, Y) plus a \code{truth} attribute.
}
\description{
Useful for testing the cross-direction Neyman pooling. The first
\code{frac_in} of treated units start at d=0 and turn on (switcher-in);
the remaining \code{1 - frac_in} start at d=1 and turn off (switcher-out).
Never-treated units stay at d=0.
}
