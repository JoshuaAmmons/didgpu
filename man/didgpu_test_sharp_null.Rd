% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/testmechs.R
\name{didgpu_test_sharp_null}
\alias{didgpu_test_sharp_null}
\title{Sharp test of full mediation}
\usage{
didgpu_test_sharp_null(
  df,
  d,
  m,
  y,
  method = c("CS", "ARP", "FSST"),
  B = 500L,
  num_Ybins = 5L,
  cluster = NULL,
  reg_formula = NULL,
  alpha = 0.05,
  seed = 1L,
  backend = "auto"
)
}
\arguments{
\item{df}{A data.frame. Cross-sectional; one row per observation.}

\item{d}{Character. Column name of the binary treatment (0/1).}

\item{m}{Character. Column name of the discrete mediator.}

\item{y}{Character. Column name of the outcome (continuous outcomes
are binned to \code{num_Ybins} quantile bins).}

\item{method}{One of \code{"CS"} (Cox-Shi), \code{"ARP"} (Andrews-Roth-Pakes),
or \code{"FSST"} (Fang-Santos-Shaikh-Torgovitsky). Default \code{"CS"}.}

\item{B}{Integer. Number of bootstrap resamples for variance
estimation. Default \code{500L}.}

\item{num_Ybins}{Integer or NULL. For continuous \code{y}, the number of
quantile bins to use. Default \code{5L}. Ignored when \code{y} is already
discrete.}

\item{cluster}{Character or NULL. Column name for cluster bootstrap.}

\item{reg_formula}{Optional one-sided formula for regression-adjusted
partial densities.}

\item{alpha}{Numeric. Test level. Default \code{0.05}.}

\item{seed}{Integer. RNG seed.}

\item{backend}{One of \code{"auto"}, \code{"r"}, \code{"cuda"}. \code{"auto"} picks
\code{"cuda"} when available.}
}
\value{
A list with \code{reject} (logical), \code{test_stat} (numeric),
\code{cv} (critical value), \code{pval} (numeric), \code{method} (character).
}
\description{
Tests whether a binary treatment \code{D} affects an outcome \code{Y} only
through a discrete mediator \code{M}. The null hypothesis is sharp full
mediation: \code{Y(1, m) = Y(0, m)} for every level \code{m}. Implemented via
three moment-inequality testing procedures.
}
\examples{
\dontrun{
# NOT YET IMPLEMENTED. The example below shows the planned API.
df <- data.frame(D = sample(0:1, 1000, TRUE),
                 M = sample(1:3, 1000, TRUE),
                 Y = rnorm(1000))
res <- didgpu_test_sharp_null(df, "D", "M", "Y", method = "CS",
                                B = 500L, seed = 1L)
res$pval
}
}
\references{
Kwon, S. and Roth, J. (2026). "(Empirical) Bayes approaches to
parallel trends." \emph{Review of Economic Studies}, forthcoming.
}
