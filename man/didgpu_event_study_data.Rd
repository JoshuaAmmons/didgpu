% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/event_study.R
\name{didgpu_event_study_data}
\alias{didgpu_event_study_data}
\title{Extract long-format event-study data from a didgpu_result}
\usage{
didgpu_event_study_data(x)
}
\arguments{
\item{x}{A \code{didgpu_result}.}
}
\value{
A data.frame.
}
\description{
Returns one row per event-time (placebos at negative k, effects at
k = 1..effects), with columns \code{event_time}, \code{estimate}, \code{std.error},
\code{conf.low}, \code{conf.high}, \code{kind}. Suitable for direct plotting with
ggplot2 — e.g.:
}
\details{
\preformatted{
  library(ggplot2)
  ggplot(didgpu_event_study_data(fit),
         aes(event_time, estimate)) +
    geom_pointrange(aes(ymin = conf.low, ymax = conf.high)) +
    geom_hline(yintercept = 0, linetype = "dashed")
}

Note: placebos are conventionally plotted at "negative" event time
(\verb{-1, -2, ...}) even though they are computed at horizon \verb{1, 2, ...}
from the switch. The \code{event_time} column follows the convention.
}
\examples{
p <- didgpu_simulate_panel(n_units = 40L, n_periods = 10L, seed = 17L)
fit <- didgpu(p, "Y", "unit", "period", "D",
               effects = 2L, placebo = 1L,
               bootstrap_reps = 0L, backend = "r", verbose = FALSE)
didgpu_event_study_data(fit)
}
