% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/honest_did.R
\name{didgpu_honest_did}
\alias{didgpu_honest_did}
\title{Sensitivity analysis for a fitted event-study DiD}
\usage{
didgpu_honest_did(
  fit,
  event_post = 1L,
  method = c("RM", "M"),
  Mbar = c(0, 0.5, 1, 1.5, 2),
  alpha = 0.05,
  ci_level = 100 * (1 - alpha)
)
}
\arguments{
\item{fit}{A \code{didgpu_result} (with placebos > 0) or \code{didgpu_cs_result}
(which always includes pre-treatment placebos automatically).}

\item{event_post}{Integer. Which post-treatment event-time to bound.
Default \code{1L}.}

\item{method}{One of \code{"M"} (smoothness; bound consecutive-period
violations) or \code{"RM"} (relative magnitudes; bound by Mbar times
the largest pre-trend deviation). Default \code{"RM"}.}

\item{Mbar}{Numeric vector. Grid of restriction values to test.
For \code{"M"}, this is the smoothness bound in outcome units; for
\code{"RM"}, the multiplier on the largest pre-trend.
Default \code{c(0, 0.5, 1, 1.5, 2)}.}

\item{alpha}{Numeric. Significance level. Default \code{0.05}.}

\item{ci_level}{Numeric. Same as \code{(1 - alpha) * 100}. Convenience.}
}
\value{
A \code{didgpu_honest_did_result}: data.frame with one row per
Mbar value: \code{Mbar}, \code{lb} (lower bound of robust CI), \code{ub},
\code{crosses_zero} (TRUE if CI includes 0; the conclusion is fragile).
Plus a \verb{$breakdown} attribute giving the smallest Mbar at which
the CI includes zero.
}
\description{
Given a fitted didgpu / didgpu_cs result with both pre- and post-
treatment event-study coefficients, runs the Rambachan-Roth (2023)
sensitivity analysis: bound the post-treatment estimate under a
user-specified restriction on the magnitude of pre-trend violations,
and report the "breakdown" parameter -- the smallest violation
that would flip the substantive conclusion.
}
\examples{
\donttest{
p <- didgpu_simulate_panel(n_units = 100L, n_periods = 12L,
                            tau_profile = c(0.5, 1.0),
                            seed = 17L)
fit <- didgpu_cs(p, "Y", "unit", "period", "D",
                  est_method = "OR", bootstrap_reps = 30L,
                  backend = "r", verbose = FALSE)
sens <- didgpu_honest_did(fit, event_post = 1L,
                           method = "RM",
                           Mbar = c(0, 0.5, 1.0))
print(sens)
}
}
\references{
Rambachan, A. and Roth, J. (2023). "A More Credible Approach to
Parallel Trends." \emph{Review of Economic Studies} 90(5): 2555-2591.
}
