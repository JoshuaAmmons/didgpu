% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/loo.R
\name{didgpu_loo}
\alias{didgpu_loo}
\title{Leave-one-out (LOO) robustness analysis}
\usage{
didgpu_loo(fit, by = "cohort", df = NULL, verbose = TRUE)
}
\arguments{
\item{fit}{A fitted \code{didgpu_result}, \code{didgpu_cs_result}, or
\code{didgpu_fect_result}.}

\item{by}{Either \code{"cohort"} (drop each treatment cohort in turn),
\code{"unit"} (drop each unit), \code{"cluster"} (drop each cluster as
defined by the fit's \code{cluster} arg if set, otherwise \code{group}),
or a character giving a column name to use for the drop key.}

\item{df}{Optional original panel. If \code{NULL}, the function looks
for an attached panel hash + path in the fit; if not found, you
must supply \code{df}. Standard pattern: pass the same \code{df} you fit
with.}

\item{verbose}{Logical. Print one line per leave-out fit.}
}
\value{
An object of class \code{didgpu_loo_result}: a data.frame with
columns \code{leave_out} (the entity dropped), \code{estimate} (the
headline under that drop), \code{delta} (estimate - full-sample
estimate), \code{delta_pct} (delta as \% of full-sample). Sorted by
\code{abs(delta)} descending. Plus \verb{$full} attribute (the full-sample
estimate) and \verb{$by} attribute.
}
\description{
For each entity (cohort / unit / cluster / level of a column),
re-fits the estimator dropping that entity and reports the
headline estimate. The headline depends on family:
\itemize{
\item \code{didgpu_result}: the ATE.
\item \code{didgpu_cs_result}: the requested aggregation's first row
(or the \code{overall} value if aggregation = "overall").
\item \code{didgpu_fect_result}: the ATE.
}
}
\details{
Useful for detecting single-cohort or single-unit influence on
headline estimates.
}
\examples{
\donttest{
p <- didgpu_simulate_panel(n_units = 80L, n_periods = 12L,
                            tau_profile = c(0.5, 1.0), seed = 17L)
fit <- didgpu_cs(p, "Y", "unit", "period", "D",
                  est_method = "OR", aggregation = "overall",
                  bootstrap_reps = 0L, backend = "r", verbose = FALSE)
loo <- didgpu_loo(fit, by = "cohort", df = p, verbose = FALSE)
print(loo)
}
}
