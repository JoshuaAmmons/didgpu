% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/testmechs.R
\name{didgpu_lb_frac_affected}
\alias{didgpu_lb_frac_affected}
\title{Sharp lower bound on the fraction of always-takers affected}
\usage{
didgpu_lb_frac_affected(
  df,
  d,
  m,
  y,
  B = 500L,
  at_group = NULL,
  num_Ybins = 5L,
  cluster = NULL,
  reg_formula = NULL,
  alpha = 0.05,
  seed = 1L,
  backend = "auto"
)
}
\arguments{
\item{df}{A data.frame. Cross-sectional; one row per observation.}

\item{d}{Character. Column name of the binary treatment (0/1).}

\item{m}{Character. Column name of the discrete mediator.}

\item{y}{Character. Column name of the outcome (continuous outcomes
are binned to \code{num_Ybins} quantile bins).}

\item{B}{Number of bootstrap resamples for the CI. Default \code{500L}.}

\item{at_group}{Optional character: name of an always-taker subgroup
column. NULL pools over all groups.}

\item{num_Ybins}{Integer or NULL. For continuous \code{y}, the number of
quantile bins to use. Default \code{5L}. Ignored when \code{y} is already
discrete.}

\item{cluster}{Character or NULL. Column name for cluster bootstrap.}

\item{reg_formula}{Optional one-sided formula for regression-adjusted
partial densities.}

\item{alpha}{Numeric. Test level. Default \code{0.05}.}

\item{seed}{Integer. RNG seed.}

\item{backend}{One of \code{"auto"}, \code{"r"}, \code{"cuda"}. \code{"auto"} picks
\code{"cuda"} when available.}
}
\value{
A list with \code{lb} (lower bound), \code{ci_low}, \code{ci_high},
\code{method = "TV"}.
}
\description{
Given a binary treatment \code{D}, a discrete mediator \code{M}, and an
outcome \code{Y}, computes a sharp lower bound on the fraction of
always-takers (units with \code{M(1) = M(0)}) whose outcome is moved by
the treatment. The bound is identified from the total-variation
distance between treated and control conditional distributions of
\code{Y} given \code{M}.
}
\examples{
\dontrun{
# NOT YET IMPLEMENTED. Planned API:
df <- data.frame(D = sample(0:1, 1000, TRUE),
                 M = sample(1:3, 1000, TRUE),
                 Y = rnorm(1000))
lb <- didgpu_lb_frac_affected(df, "D", "M", "Y", B = 500L)
lb$lb; lb$ci_low; lb$ci_high
}
}
