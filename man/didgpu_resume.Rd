% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checkpoint.R
\name{didgpu_resume}
\alias{didgpu_resume}
\title{Quick resume helper}
\usage{
didgpu_resume(checkpoint_dir, df, ...)
}
\arguments{
\item{checkpoint_dir}{Path to existing checkpoint directory.}

\item{df}{The same panel originally passed (validated by hash).}

\item{...}{Optional overrides forwarded to \code{didgpu()}.}
}
\value{
The aggregated result, same shape as \code{didgpu()}.
}
\description{
Equivalent to calling \code{didgpu(...)} with the same arguments and
\verb{checkpoint_dir = checkpoint_dir, resume = TRUE}, but pulls the
arguments from \code{meta.json} so the caller does not have to repeat
them. The panel must still be supplied (panels are not stored on
disk; only the panel hash, for integrity checks).
}
\details{
All optional arguments may be overridden by passing them through
\code{...}; the override takes precedence over the checkpointed value.
Use this to (for example) bump \code{bootstrap_reps} higher or switch
\code{backend} mid-run — but note that mismatches on identity-relevant
fields (outcome, group, time, treatment, effects, placebo, seed)
will be rejected by \code{.check_meta_compatibility()} inside \code{didgpu()}.
}
\examples{
\donttest{
p <- didgpu_simulate_panel(n_units = 40L, n_periods = 10L,
                            tau_profile = c(0.5, 1.0), seed = 17L)
cdir <- tempfile("didgpu_resume_demo_")
fit1 <- didgpu(p, "Y", "unit", "period", "D",
                effects = 2L, bootstrap_reps = 3L, seed = 1L,
                checkpoint_dir = cdir, backend = "r", verbose = FALSE)
# Resume with no extra work — produces the identical result.
fit2 <- didgpu_resume(cdir, df = p)
identical(coef(fit1), coef(fit2))
unlink(cdir, recursive = TRUE)
}
}
