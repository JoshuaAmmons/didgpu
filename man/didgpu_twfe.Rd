% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/twfe.R
\name{didgpu_twfe}
\alias{didgpu_twfe}
\title{Naive two-way fixed-effects (TWFE) dynamic event study}
\usage{
didgpu_twfe(df, outcome, group, time, treatment, effects = 1L,
  placebo = 0L, cluster = NULL, tol = 1e-10, max_iter = 1000L,
  verbose = TRUE)
}
\arguments{
\item{df}{A data.frame / data.table panel.}

\item{outcome, group, time, treatment}{Character column names: the outcome,
unit id, time id, and (binary, possibly non-absorbing) treatment indicator.}

\item{effects}{Integer >= 1. Number of post/contemporaneous lag terms
(Effect_1 = contemporaneous, Effect_k = lag k-1).}

\item{placebo}{Integer >= 0. Number of lead terms (Placebo_j = lead j), the
pre-trend / anticipation checks.}

\item{cluster}{Optional character column name to cluster SEs on. Defaults to
\code{group}.}

\item{tol}{Convergence tolerance for the iterative two-way fixed-effect
demeaning. Default \code{1e-10}.}

\item{max_iter}{Max demeaning iterations. Default \code{1000}.}

\item{verbose}{Logical.}
}
\value{
An object of class \code{didgpu_twfe_result}: a list with \code{coef}
(named vector), \code{results} (with \code{Effects} and \code{Placebos}
matrices: Estimate / SE / LB.CI / UB.CI / N), and \code{args}.
}
\description{
Fits the distributed-lag TWFE specification with unit and time fixed effects
and cluster-robust standard errors. Intended as the "naive" baseline to
report next to the bias-robust estimators \code{\link[=didgpu]{didgpu()}} and
\code{\link[=didgpu_cs]{didgpu_cs()}}.
}
\details{
The fixed effects are absorbed by iterative two-way demeaning (cost scales
with observations, not units), so the estimator stays fast on large panels
where building unit/time dummy matrices would be impractical. Point estimates
are bit-exact to \code{lm()} on the dummy specification; the \code{Effect_k} /
\code{Placebo_j} output mirrors \code{\link[=didgpu]{didgpu()}} so the
bias-prone TWFE estimate sits next to the robust one. TWFE is biased under
heterogeneous/dynamic effects; see \code{\link[=didgpu_bacon]{didgpu_bacon()}}
for the Goodman-Bacon decomposition of that bias.
}
\examples{
\donttest{
p <- didgpu_simulate_panel(n_units = 60L, n_periods = 10L,
                            tau_profile = c(0.5, 1.0), seed = 17L)
fit <- didgpu_twfe(p, "Y", "unit", "period", "D",
                    effects = 3L, placebo = 2L, verbose = FALSE)
print(fit)
}
}
\seealso{
\code{\link[=didgpu]{didgpu()}} and \code{\link[=didgpu_cs]{didgpu_cs()}} for
the heterogeneity-robust estimators; \code{\link[=didgpu_bacon]{didgpu_bacon()}}
for the bias decomposition.
}
