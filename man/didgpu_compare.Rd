% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare.R
\name{didgpu_compare}
\alias{didgpu_compare}
\title{Compare didgpu r-backend to the DIDmultiplegtDYN reference}
\usage{
didgpu_compare(
  df,
  outcome,
  group,
  time,
  treatment,
  effects = 1L,
  placebo = 0L,
  cluster = NULL,
  switchers = "",
  tolerance = 1e-10,
  verbose = TRUE
)
}
\arguments{
\item{df}{A data.frame (or data.table) panel. Must contain the
columns named by \code{outcome}, \code{group}, \code{time}, \code{treatment}.}

\item{outcome, group, time, treatment}{Character. Column names.}

\item{effects}{Integer. Number of post-treatment event-times to
estimate (e = 1..effects). Must be >= 1.}

\item{placebo}{Integer. Number of pre-treatment placebos. 0 to skip.}

\item{cluster}{Character or NULL. Column name for cluster bootstrap;
default NULL clusters by \code{group}.}

\item{switchers}{One of \code{""} (default — both directions),
\code{"in"} (only switcher-in units), or \code{"out"} (only switcher-out
units). Mirrors the \code{switchers} arg in DIDmultiplegtDYN.}

\item{tolerance}{Numeric. Threshold above which a disagreement is
flagged as a failure. Default \code{1e-10} (effectively machine epsilon
for our pipeline).}

\item{verbose}{Logical. Print a per-column table.}
}
\value{
Invisibly a list with \code{pass} (logical scalar), \code{report}
(data.frame of per-column max diffs), \code{fit_r} and \code{fit_ref} (the
two full fits). If the reference is not installed, returns
immediately with \code{pass = NA} and a warning.
}
\description{
Runs both backends on the same panel and same call args, then reports
the max absolute disagreement per output column. Useful for verifying
didgpu produces output identical to the reference on your own data
before using it in production.
}
\examples{
\dontrun{
# Requires the DIDmultiplegtDYN package to be installed.
p <- didgpu_simulate_panel(n_units = 40L, n_periods = 10L, seed = 17L)
report <- didgpu_compare(p, "Y", "unit", "period", "D",
                          effects = 2L, placebo = 1L)
report$pass
head(report$report)
}
}
