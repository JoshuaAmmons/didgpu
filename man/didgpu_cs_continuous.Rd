% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cs_continuous.R
\name{didgpu_cs_continuous}
\alias{didgpu_cs_continuous}
\title{Callaway-Goodman-Bacon-Sant'Anna (2024) continuous-treatment DiD}
\usage{
didgpu_cs_continuous(df, yname, dname, gname, tname, idname, dvals = NULL,
  degree = 3L, num_knots = 0L, control_group = "nevertreated",
  bootstrap_reps = 200L, ci_level = 95, seed = 1L, verbose = TRUE)
}
\arguments{
\item{df}{A data.frame / data.table panel.}

\item{yname, dname, gname, tname, idname}{Character column names: outcome,
continuous dose, cohort (0 = never-treated; positive = adoption period),
time, and unit id.}

\item{dvals}{Numeric dose values at which to report the curve. Default:
quantiles 0.1..0.99 of the positive doses.}

\item{degree}{B-spline degree (default 3, cubic).}

\item{num_knots}{Number of interior knots (default 0). Knots are placed at
interior quantiles of the positive dose.}

\item{control_group}{\code{"nevertreated"} (default).}

\item{bootstrap_reps}{Multiplier-bootstrap replicates for SEs (default 200;
0 to skip).}

\item{ci_level}{Confidence level in percent (default 95).}

\item{seed}{Bootstrap RNG seed.}

\item{verbose}{Logical.}
}
\value{
A \code{didgpu_cs_continuous_result}: a list with \code{dose} (the dvals),
\code{att.d}, \code{acrt.d} (and their SEs / CIs when bootstrapped),
\code{overall_att}, \code{overall_acrt}, and \code{args}.
}
\description{
Estimates the continuous-treatment dose-response: the level effect
\eqn{ATT(d)} and the causal response (slope) \eqn{ACRT(d) = ATT'(d)} of a
continuous dose, comparing dose-\eqn{d} units to a never-treated (dose 0)
comparison group via a within-unit before/after change. The dose-response is
fit by a B-spline regression; ATT(d) subtracts the comparison mean change
and ACRT(d) is the analytic spline derivative. Standard errors come from a
unit-level multiplier bootstrap.
}
\details{
Native reimplementation: the spline basis uses \pkg{splines2} (the same
library \pkg{contdid} uses, so the basis matches bit-for-bit), but the DiD
logic, dose-response and ATT/ACRT extraction are didgpu's own. Scope: a
single treated cohort vs a never-treated comparison (the canonical
continuous-DiD design). Point estimates match \code{contdid::cont_did}
exactly.
}
\references{
Callaway, B., Goodman-Bacon, A. & Sant'Anna, P.H.C. (2024).
Difference-in-Differences with a Continuous Treatment. NBER WP 32117.
}
\examples{
\donttest{
if (requireNamespace("contdid", quietly = TRUE) &&
    requireNamespace("splines2", quietly = TRUE)) {
  d <- contdid::simulate_contdid_data(n = 400, num_time_periods = 2)
  didgpu_cs_continuous(d, "Y", "D", "G", "time_period", "id",
                       degree = 3, num_knots = 2, bootstrap_reps = 0)
}
}
}
\seealso{
\code{\link[=didgpu_cs]{didgpu_cs()}} for the binary staggered estimator.
}
