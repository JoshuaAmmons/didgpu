% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bacon.R
\name{didgpu_bacon}
\alias{didgpu_bacon}
\title{Goodman-Bacon decomposition of the TWFE DiD estimator}
\usage{
didgpu_bacon(df, outcome, group, time, treatment)
}
\arguments{
\item{df}{A data.frame / data.table panel.}

\item{outcome, group, time, treatment}{Character column names: outcome,
unit id, time id, and the binary 0/1 absorbing treatment indicator.}
}
\value{
A \code{didgpu_bacon} object: a list with \code{beta_twfe} (the static
TWFE DiD coefficient on the decomposition sample), \code{comparisons} (a
data.frame: \code{type}, \code{treated}, \code{control} timing values,
\code{estimate}, \code{weight}), \code{summary} (per-type total weight and
weighted-average 2x2 estimate), \code{forbidden_weight} (total weight on
already-treated-control comparisons), and \code{n_always_treated} (units
dropped).
}
\description{
Decomposes the static two-way fixed-effects DiD coefficient into the
weighted average of all 2x2 timing-group comparisons (Goodman-Bacon
2021). The headline diagnostic is the total weight placed on "forbidden"
comparisons that use already-treated units as controls -- the source of
TWFE bias under heterogeneous, dynamic treatment effects. Pair it with
\code{\link[=didgpu_twfe]{didgpu_twfe()}} (the estimate) and
\code{\link[=didgpu]{didgpu()}} / \code{\link[=didgpu_cs]{didgpu_cs()}}
(robust alternatives).
}
\details{
Scope: a \strong{balanced} panel with a \strong{binary, absorbing}
(staggered-adoption) treatment -- the canonical Goodman-Bacon case.
Always-treated units (treated in the first period, no pre-period) are
dropped with a note, as in the reference implementation. For unbalanced
panels, treatment that switches off, or continuous treatment, use
\code{\link[=didgpu]{didgpu()}}.
}
\examples{
\donttest{
p <- didgpu_simulate_panel(n_units = 80L, n_periods = 12L,
                           tau_profile = c(0.5, 1.0), seed = 7L)
p$D <- as.integer(p$D >= 0.5)            # binary staggered treatment
bd <- didgpu_bacon(p, "Y", "unit", "period", "D")
bd
}
}
\seealso{
\code{\link[=didgpu_twfe]{didgpu_twfe()}}, \code{\link[=didgpu]{didgpu()}},
\code{\link[=didgpu_cs]{didgpu_cs()}}.
}
