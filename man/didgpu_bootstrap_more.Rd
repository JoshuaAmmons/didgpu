% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checkpoint.R
\name{didgpu_bootstrap_more}
\alias{didgpu_bootstrap_more}
\title{Add more bootstrap reps to an existing checkpoint}
\usage{
didgpu_bootstrap_more(checkpoint_dir, df, extra_reps, ...)
}
\arguments{
\item{checkpoint_dir}{Path to existing checkpoint.}

\item{df}{The same panel originally passed (validated by hash).}

\item{extra_reps}{Integer >= 1: how many MORE reps to add.}

\item{...}{Additional arguments forwarded to \code{didgpu()} (e.g.
\code{n_workers}, \code{verbose}). Overriding identity-relevant arguments
like \code{outcome}, \code{effects}, \code{seed} will fail compatibility check.}
}
\value{
The aggregated result, same shape as \code{didgpu()}.
}
\description{
Use this when you already have, say, 100 reps committed and you
realise you want 200. Rather than throwing away the 100 you have
and starting over, this function patches the checkpoint's recorded
\code{bootstrap_reps} upward and re-invokes \code{didgpu()} with
\code{resume = TRUE}, so only the new (\code{extra_reps}) cells are computed.
}
\details{
The new cells use seeds \verb{seed + (current_reps + 1)..(current_reps + extra_reps)},
which is exactly what \code{didgpu()} would have done on a fresh run with
the larger bootstrap count. The result is therefore identical to
starting from scratch with the larger count.
}
\examples{
\donttest{
p <- didgpu_simulate_panel(n_units = 40L, n_periods = 10L,
                            tau_profile = c(0.5, 1.0), seed = 17L)
cdir <- tempfile("didgpu_more_demo_")
didgpu(p, "Y", "unit", "period", "D",
        effects = 2L, bootstrap_reps = 3L, seed = 1L,
        checkpoint_dir = cdir, backend = "r", verbose = FALSE)
# Add 2 more bootstrap reps without redoing the first 3.
fit <- didgpu_bootstrap_more(cdir, df = p, extra_reps = 2L,
                               verbose = FALSE)
fit$args$bootstrap_reps   # now 5
unlink(cdir, recursive = TRUE)
}
}
