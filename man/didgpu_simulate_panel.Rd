% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{didgpu_simulate_panel}
\alias{didgpu_simulate_panel}
\title{Generate a simulated DiD panel with known event-time profile}
\usage{
didgpu_simulate_panel(
  n_units = 100L,
  n_periods = 20L,
  frac_treated = 0.5,
  min_treat_period = NULL,
  max_treat_period = NULL,
  tau_profile = c(0.2, 0.4, 0.6, 0.5, 0.4),
  sigma = 0.5,
  unit_fe_sd = 1,
  time_fe_sd = 0.3,
  seed = 1L,
  late_entry_frac = 0,
  max_entry_period = NULL
)
}
\arguments{
\item{n_units}{integer. Number of units.}

\item{n_periods}{integer. Number of time periods (1..n_periods).}

\item{frac_treated}{numeric between 0 and 1. Share of units that ever get
treated. The rest are never-treated controls.}

\item{min_treat_period}{integer. Earliest treatment-on period.}

\item{max_treat_period}{integer. Latest treatment-on period. Must be
\verb{<= n_periods}. Treated units' \code{F_g} is drawn uniformly between
\code{min_treat_period} and \code{max_treat_period}.}

\item{tau_profile}{numeric vector. Event-time effects:
\code{tau_profile[k+1]} is the effect at event time \verb{k = 0, 1, ...}.
Length \code{length(tau_profile) - 1} upper bound on k.}

\item{sigma}{numeric. Idiosyncratic noise SD.}

\item{unit_fe_sd}{numeric. Unit fixed-effect SD.}

\item{time_fe_sd}{numeric. Time fixed-effect SD.}

\item{seed}{integer. RNG seed.}

\item{late_entry_frac}{numeric in \verb{[0, 1]}. Share of units that enter
the panel after period 1, producing an UNBALANCED panel: their rows
before entry are absent entirely (not \code{NA}). Default \code{0} (balanced),
which leaves the RNG stream untouched so every existing seeded
fixture stays bit-identical.}

\item{max_entry_period}{integer. Latest entry period for late
entrants, drawn uniformly from \code{2:max_entry_period}. Defaults to
\code{max(2, floor(n_periods * 0.4))}, which keeps most late entrants
with a usable pre-period before they switch.}
}
\value{
A data.frame with columns: \code{unit} (int), \code{period} (int),
\code{D} (binary treatment indicator), \code{Y} (outcome). Sorted by
(unit, period). Also has an attribute \code{"truth"}: a list with
\code{F_g} (named numeric per unit; Inf = never-treated),
\code{tau_profile}, \code{unit_fe}, \code{time_fe}, and \code{entry_period} (named
integer per late-entering unit, or \code{NULL} when balanced).
}
\description{
Generate a simulated DiD panel with known event-time profile
}
\examples{
p <- didgpu_simulate_panel(n_units = 40L, n_periods = 10L,
                            tau_profile = c(0.5, 1.0, 1.2),
                            seed = 17L)
head(p)
# Inspect the underlying DGP:
truth <- attr(p, "truth")
table(truth$F_g)            # cohort sizes (Inf = never-treated)
truth$tau_profile
}
