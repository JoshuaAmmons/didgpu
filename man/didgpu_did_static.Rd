% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/did_static.R
\name{didgpu_did_static}
\alias{didgpu_did_static}
\title{de Chaisemartin-D'Haultfoeuille (2020) DID_M instantaneous estimator}
\usage{
didgpu_did_static(df, outcome, group, time, treatment, weight = NULL,
  bootstrap_reps = 100L, cluster = NULL, ci_level = 95, seed = 1L,
  verbose = TRUE)
}
\arguments{
\item{df}{A data.frame / data.table panel.}

\item{outcome, group, time, treatment}{Character column names: outcome, unit
id, time id, and a binary 0/1 (possibly non-absorbing) treatment.}

\item{weight}{Optional character column name of observation weights.}

\item{bootstrap_reps}{Integer; cluster-bootstrap replicates for the SE
(0 to skip and return only the point estimate). Default 100.}

\item{cluster}{Optional character column name to resample in the bootstrap.
Defaults to \code{group}.}

\item{ci_level}{Confidence level in percent (default 95).}

\item{seed}{RNG seed for the bootstrap.}

\item{verbose}{Logical.}
}
\value{
A \code{didgpu_did_static_result}: a list with \code{did} (the DID_M point
estimate), \code{se}, \code{ci}, \code{n_switchers}, \code{per_period}
(per-period switcher counts and directional DiDs), and \code{args}.
}
\description{
The DID_M estimator for a binary treatment that may switch on AND off
(non-absorbing). It compares each switching unit's period-over-period
outcome change to that of stayers with the same prior-period treatment, and
averages over all switch events. Consistent for the ATT among switchers
under common trends + no anticipation, even with heterogeneous and dynamic
treatment effects (where TWFE is biased). Standard errors are from a cluster
(by default unit) bootstrap, matching the package's other estimators.
}
\references{
de Chaisemartin, C. & D'Haultfoeuille, X. (2020). Two-Way Fixed Effects
Estimators with Heterogeneous Treatment Effects. \emph{American Economic
Review} 110(9): 2964-2996.
}
\examples{
\donttest{
p <- didgpu_simulate_panel(n_units = 80L, n_periods = 8L,
                           tau_profile = c(0.5, 1.0), seed = 5L)
p$D <- as.integer(p$D >= 0.5)
didgpu_did_static(p, "Y", "unit", "period", "D", bootstrap_reps = 0L)
}
}
\seealso{
\code{\link[=didgpu]{didgpu()}} for the dynamic generalization,
\code{\link[=didgpu_twfe]{didgpu_twfe()}} / \code{\link[=didgpu_bacon]{didgpu_bacon()}}
for the biased TWFE baseline and its decomposition.
}
