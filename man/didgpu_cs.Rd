% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cs.R
\name{didgpu_cs}
\alias{didgpu_cs}
\title{Estimate Callaway-Sant'Anna (2021) staggered-treatment DiD}
\usage{
didgpu_cs(
  df,
  outcome,
  group,
  time,
  treatment,
  control_group = c("never", "notyet"),
  est_method = c("OR", "IPW", "DR"),
  aggregation = c("event", "group", "calendar", "overall"),
  covariates = NULL,
  bootstrap_reps = 0L,
  bootstrap_kind = c("cluster", "multiplier"),
  ci_level = 95,
  seed = 1L,
  backend = "auto",
  verbose = TRUE
)
}
\arguments{
\item{df}{A panel data.frame.}

\item{outcome, group, time, treatment}{Column names. \code{treatment} must be
binary (0/1).}

\item{control_group}{Either \code{"never"} (never-treated; default) or
\code{"notyet"} (not-yet-treated).}

\item{est_method}{One of \code{"OR"}, \code{"IPW"}, \code{"DR"}. Default \code{"OR"}.}

\item{aggregation}{One of \code{"event"}, \code{"group"}, \code{"calendar"},
\code{"overall"}. Default \code{"event"}. Use \code{\link[=didgpu_cs_aggregate]{didgpu_cs_aggregate()}} to
compute additional aggregations from a single fit.}

\item{covariates}{Optional character vector of time-invariant
covariate column names for OR / IPW / DR adjustment.}

\item{bootstrap_reps}{Integer. Number of bootstrap reps for SE
estimation. Default \code{0L} (point estimate only).}

\item{bootstrap_kind}{One of \code{"cluster"} (resample units, refit per
rep) or \code{"multiplier"} (multiplier wild bootstrap on the
influence functions; much faster for large B). Default \code{"cluster"}.}

\item{ci_level}{Numeric in (0, 100). Default \code{95}.}

\item{seed}{Integer.}

\item{backend}{One of \code{"auto"}, \code{"r"}, \code{"cuda"}.}

\item{verbose}{Logical. Print progress per (g, t).}
}
\value{
An object of class \code{didgpu_cs_result}:
\itemize{
\item \code{att_gt}: long-form data.frame of ATT(g, t) estimates.
\item \code{aggregation}: the chosen aggregation summary
(event-study / group / calendar / overall).
\item \code{args}: the canonical args bundle (for re-aggregation).
}
}
\description{
Estimates group-time average treatment effects ATT(g, t) for each
cohort g (defined by first-treatment period) and each post-treatment
time t >= g, then aggregates into the requested summary.
}
\details{
Three inner estimators (\code{est_method}):
\itemize{
\item \code{"OR"} (outcome regression, default): fit a linear model of
the outcome change on covariates among controls only;
predict counterfactual for treated; ATT = mean of
(observed - predicted) over treated cohort cells.
\item \code{"IPW"} (inverse-probability weighting): weight treated and
control cells by inverse propensity score.
\item \code{"DR"} (doubly robust): combines OR and IPW; consistent if
either model is correct. Preferred in practice.
}

Four aggregation schemes (\code{aggregation}):
\itemize{
\item \code{"event"} (default): event-study, indexed by event-time e = t - g.
\item \code{"group"}: per-cohort average effect over post-treatment periods.
\item \code{"calendar"}: per-calendar-time average over cohorts already treated.
\item \code{"overall"}: single scalar summary, the weighted average of
all post-treatment (g, t) cells.
}
}
\examples{
\donttest{
p <- didgpu_simulate_panel(n_units = 100L, n_periods = 12L,
                            tau_profile = c(0.5, 1.0), seed = 17L)
fit <- didgpu_cs(p, "Y", "unit", "period", "D",
                  est_method = "OR", aggregation = "event",
                  bootstrap_reps = 0L, backend = "r", verbose = FALSE)
print(fit)
}
}
\references{
Callaway, B. and Sant'Anna, P. (2021). "Difference-in-Differences
with multiple time periods." \emph{Journal of Econometrics} 225(2): 200-230.
}
