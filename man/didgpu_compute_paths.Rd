% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/by_path.R
\name{didgpu_compute_paths}
\alias{didgpu_compute_paths}
\title{Add a per-group treatment-trajectory column to a panel}
\usage{
didgpu_compute_paths(
  df,
  outcome,
  group,
  time,
  treatment,
  effects = 1L,
  top_n = NULL
)
}
\arguments{
\item{df}{A panel data.frame.}

\item{outcome, group, time, treatment}{Column names (we only need them
to compute F_g consistently with \code{didgpu()}).}

\item{effects}{Integer. Number of post-switch periods to encode in
the path (i.e. the path string has length \code{effects + 1}).}

\item{top_n}{Integer or NULL. If non-NULL, keep only the \code{top_n}
most-common paths; groups with rarer paths are tagged with
\code{NA_character_} for the path column. NULL = keep every distinct
path.}
}
\value{
The input data.frame with a new column \code{path} of type
character. Order of rows preserved. Groups with no observed
treatment trajectory get \code{NA}.
}
\description{
For each group, builds a comma-separated string of the treatment
values observed at (F_g - 1, F_g, F_g + 1, ..., F_g + effects - 1):
the baseline period plus the first \code{effects} post-switch periods.
Groups with no switch (\code{F_g > T_max}) get a "no-switch" path string.
}
\details{
Use the resulting \code{path} column as a \code{by_var} to estimate treatment
effects separately per trajectory (e.g. via \code{didgpu_by()}).
}
\examples{
p <- didgpu_simulate_panel(n_units = 60L, n_periods = 12L,
                            tau_profile = c(0.5, 1.0),
                            seed = 17L)
aug <- didgpu_compute_paths(p, "Y", "unit", "period", "D",
                             effects = 2L)
table(aug$path, useNA = "ifany")
}
