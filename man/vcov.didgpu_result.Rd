% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggregate.R
\name{vcov.didgpu_result}
\alias{vcov.didgpu_result}
\title{Variance-covariance matrix of estimates}
\usage{
\method{vcov}{didgpu_result}(object, ...)
}
\arguments{
\item{object}{A \code{didgpu_result} object.}

\item{...}{Unused.}
}
\value{
A square matrix.
}
\description{
Returns the empirical covariance matrix of the bootstrap replicate
distribution over \verb{(Effects, Placebos)}, computed at fit time and
stored on the result. When \code{bootstrap_reps = 0} (or only one rep),
returns a square NA matrix because the covariance is undefined.
}
\details{
The ordering matches \code{coef(object)}'s default (effects first, then
placebos). The ATE row/column is NOT included — it is a linear
combination of the per-event-time effects, so its variance can be
recovered as \code{t(w) \%*\% vcov(object) \%*\% w} where \code{w} is the
incidence-weighted vector.
}
