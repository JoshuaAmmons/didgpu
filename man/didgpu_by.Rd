% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/by.R
\name{didgpu_by}
\alias{didgpu_by}
\title{Estimate didgpu separately for each level of a grouping variable}
\usage{
didgpu_by(
  df,
  by_var,
  outcome,
  group,
  time,
  treatment,
  ...,
  checkpoint_dir = NULL,
  verbose = TRUE
)
}
\arguments{
\item{df}{A panel data.frame.}

\item{by_var}{Character. Name of the grouping column. Each row in
\code{df} must have exactly one value of this column. Levels must be
coercible to character (used as subgroup labels and as
subdirectory names under \code{checkpoint_dir}, if set).}

\item{outcome, group, time, treatment}{Forwarded to \code{\link[=didgpu]{didgpu()}}.}

\item{...}{Additional arguments forwarded to \code{\link[=didgpu]{didgpu()}} (e.g.
\code{effects}, \code{placebo}, \code{bootstrap_reps}, \code{normalized}).}

\item{checkpoint_dir}{If non-NULL, each subgroup writes into a
subdirectory of this path named after the level.}

\item{verbose}{Logical. Print one line per subgroup as it starts.}
}
\value{
An object of class \code{didgpu_by_result} — a named list of
\code{didgpu_result} objects, one per subgroup, plus an attribute
\code{by_var} recording the grouping column name.
}
\description{
Wrapper that splits the panel by the unique values of \code{by_var} and
runs \code{\link[=didgpu]{didgpu()}} on each subset, returning a named list of
\code{didgpu_result} objects. All other arguments are forwarded
identically to each per-level call.
}
\details{
If \code{checkpoint_dir} is supplied, each subgroup writes into
\verb{checkpoint_dir/<level>/}. Resume works the same way as a normal
\code{\link[=didgpu]{didgpu()}} call inside each subgroup directory.
}
\examples{
p <- didgpu_simulate_panel(n_units = 40L, n_periods = 10L,
                            tau_profile = c(0.5, 1.0),
                            seed = 17L)
p$region <- ifelse(p$unit \%\% 2L == 0L, "north", "south")
fit_by <- didgpu_by(p, "region",
                     outcome = "Y", group = "unit",
                     time = "period", treatment = "D",
                     effects = 2L, bootstrap_reps = 0L,
                     backend = "r", verbose = FALSE)
print(fit_by)
}
