% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/freyaldenhoven.R
\name{didgpu_freyaldenhoven}
\alias{didgpu_freyaldenhoven}
\title{Freyaldenhoven-Hansen-Shapiro (2019) pre-event proxy event study}
\usage{
didgpu_freyaldenhoven(df, outcome, policy, id, time,
  estimator = c("OLS", "FHS"), proxy = NULL, proxyIV = NULL, pre = 0L,
  post = 1L, overidpre = pre + post, overidpost = 1L,
  normalize = -(pre + 1L), cluster = NULL, tol = 1e-10, max_iter = 1000L,
  verbose = TRUE)
}
\arguments{
\item{df}{A data.frame / data.table panel.}

\item{outcome, policy, id, time}{Character column names: outcome, the (binary
or continuous) policy variable, unit id, and integer time.}

\item{estimator}{\code{"OLS"} (default) or \code{"FHS"}.}

\item{proxy}{For \code{"FHS"}, the character name of the proxy covariate.}

\item{proxyIV}{For \code{"FHS"}, the instrument column. Default: the
first-differenced policy lead with the strongest first-stage F.}

\item{pre, post}{Non-negative integers: anticipation leads (\code{pre}) and
dynamic lags (\code{post}).}

\item{overidpre, overidpost}{Extra leads/lags (over-identification /
endpoints). Defaults mirror eventstudyr (\code{overidpost = 1},
\code{overidpre = pre + post}).}

\item{normalize}{Event-time coefficient to omit (normalized to 0). Default
\code{-(pre + 1)}.}

\item{cluster}{Optional unit-level cluster column for SEs. Defaults to \code{id}.}

\item{tol, max_iter}{Two-way demeaning controls.}

\item{verbose}{Logical.}
}
\value{
A \code{didgpu_freyaldenhoven_result}: a list with \code{coefficients} (a
matrix: Estimate / SE / LB.CI / UB.CI per event-study term, plus the proxy
for FHS), \code{estimator}, \code{proxyIV}, and \code{args}.
}
\description{
Panel event-study estimator that uses an auxiliary covariate (a "proxy"
affected by the confound but not the policy) to correct for confounding
pre-trends. \code{estimator = "OLS"} is the plain two-way FE event study;
\code{estimator = "FHS"} adds the proxy as an endogenous regressor and
instruments it (2SLS) with a far policy lead, purging the confound.
Reimplements \code{eventstudyr::EventStudy}'s first-difference
parameterization, so the event-study coefficients match that reference.
}
\references{
Freyaldenhoven, S., Hansen, C. & Shapiro, J.M. (2019). Pre-event Trends in
the Panel Event-Study Design. \emph{American Economic Review} 109(9):
3307-3338.
}
\examples{
\donttest{
if (requireNamespace("eventstudyr", quietly = TRUE)) {
  d <- eventstudyr::example_data
  didgpu_freyaldenhoven(d, "y_base", "z", "id", "t",
                        estimator = "FHS", proxy = "x_r", pre = 0, post = 3)
}
}
}
