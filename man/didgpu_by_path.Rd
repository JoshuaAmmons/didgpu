% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/by_path.R
\name{didgpu_by_path}
\alias{didgpu_by_path}
\title{Run didgpu separately per treatment trajectory}
\usage{
didgpu_by_path(
  df,
  outcome,
  group,
  time,
  treatment,
  effects = 1L,
  top_n = NULL,
  ...,
  checkpoint_dir = NULL,
  verbose = TRUE
)
}
\arguments{
\item{df}{A panel data.frame.}

\item{outcome, group, time, treatment}{Column names (we only need them
to compute F_g consistently with \code{didgpu()}).}

\item{effects}{Integer. Number of post-switch periods to encode in
the path (i.e. the path string has length \code{effects + 1}).}

\item{top_n}{Integer or NULL. If non-NULL, keep only the \code{top_n}
most-common paths; groups with rarer paths are tagged with
\code{NA_character_} for the path column. NULL = keep every distinct
path.}

\item{...}{Additional arguments forwarded to \code{didgpu()} (e.g.
\code{placebo}, \code{bootstrap_reps}, \code{normalized}).}

\item{checkpoint_dir}{If non-NULL, each subgroup writes into a
subdirectory of this path named after the path string.}

\item{verbose}{Logical. Print one line per subgroup as it starts.}
}
\value{
A \code{didgpu_by_result}: named list of \code{didgpu_result} objects,
one per path.
}
\description{
Wrapper: calls \code{didgpu_compute_paths()} to add a \code{path} column to the
panel, then runs \code{didgpu_by(df, by_var = "path", ...)} on the result.
Subgroups (rows with \code{path = NA} because they're not in \code{top_n}) are
dropped before estimation.
}
\examples{
\donttest{
p <- didgpu_simulate_panel(n_units = 100L, n_periods = 12L,
                            tau_profile = c(0.5, 1.0),
                            seed = 17L)
fit_by_path <- didgpu_by_path(
  p, outcome = "Y", group = "unit", time = "period", treatment = "D",
  effects = 2L, top_n = 3L,
  bootstrap_reps = 0L, backend = "r", verbose = FALSE
)
print(fit_by_path)
}
}
