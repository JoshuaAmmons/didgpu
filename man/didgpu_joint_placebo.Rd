% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/equivalence.R
\name{didgpu_joint_placebo}
\alias{didgpu_joint_placebo}
\title{Windowed / subset joint test of pre-treatment placebos}
\usage{
didgpu_joint_placebo(x, horizons = NULL)
}
\arguments{
\item{x}{A \code{didgpu_result} from \code{\link[=didgpu]{didgpu()}} run with \code{placebo > 0} and
\code{bootstrap_reps > 0}.}

\item{horizons}{Integer vector of placebo horizons to include, as
positive distances before treatment (1 = the period immediately
pre-treatment, i.e. event time -1, matching \code{Placebo_1}). Negative
values are accepted and used by magnitude. \code{NULL} (default) uses all
placebo horizons, reproducing \code{fit$results$p_jointplacebo}.}
}
\value{
A \code{didgpu_joint_placebo} object: a list with \code{statistic}
(chi-square), \code{df}, \code{p_value}, \code{horizons} (the included event times,
negative), \code{estimates} (the included placebo point estimates), and
\code{n_boot}.
}
\description{
Runs the same chi-square joint Wald test as \code{fit$results$p_jointplacebo}
but on a chosen subset of placebo horizons, using the bootstrap
covariance \code{\link[=didgpu]{didgpu()}} already stored. Useful when parallel trends only
need to hold over a specific pre-treatment window.
}
\examples{
\donttest{
p <- didgpu_simulate_panel(n_units = 80L, n_periods = 12L,
                           tau_profile = c(0.5, 1.0), seed = 7L)
p$D <- as.integer(p$D >= 0.5)
fit <- didgpu(p, "Y", "unit", "period", "D", effects = 3L, placebo = 3L,
              bootstrap_reps = 200L, verbose = FALSE)
didgpu_joint_placebo(fit, horizons = 1:2)   # only the two nearest leads
}
}
\seealso{
\code{\link[=didgpu_equivalence]{didgpu_equivalence()}} for an equivalence (TOST) framing of the
same placebos.
}
