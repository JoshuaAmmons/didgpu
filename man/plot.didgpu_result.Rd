% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggregate.R
\name{plot.didgpu_result}
\alias{plot.didgpu_result}
\title{Event-study plot of a didgpu result}
\usage{
\method{plot}{didgpu_result}(x, ..., show_zero_line = TRUE, show_zero_horizon = TRUE, ci = TRUE)
}
\arguments{
\item{x}{A \code{didgpu_result} object.}

\item{...}{Extra graphical parameters passed to the underlying
\code{plot()} call (e.g. \code{main}, \code{xlab}, \code{ylab}, \code{col}, \code{pch}, \code{lwd},
\code{xlim}, \code{ylim}).}

\item{show_zero_line}{Logical. Draw a dashed line at y = 0
(default TRUE).}

\item{show_zero_horizon}{Logical. Mark the boundary between placebo
and effect horizons with a vertical dashed line (default TRUE).}

\item{ci}{Logical. Draw error bars at the stored CI level
(default TRUE; suppressed when bootstrap_reps = 0 because the
CIs are NA).}
}
\value{
The input invisibly.
}
\description{
Plots estimates against event-time horizon: pre-treatment placebos
at negative horizons, post-treatment effects at positive horizons,
with the stored CIs as vertical error bars and a horizontal dashed
line at 0 for reference. Uses base R graphics — no ggplot2
dependency. Returns the input invisibly so calls can be chained.
}
