% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fect_robustness.R
\name{didgpu_fect_placebo}
\alias{didgpu_fect_placebo}
\title{Run placebo (pre-treatment) tests on a fitted fect model}
\usage{
didgpu_fect_placebo(
  df,
  outcome,
  group,
  time,
  treatment,
  method = c("fe", "ife", "mc"),
  n_placebos = 3L,
  r = 2L,
  lambda = NULL,
  tol = 1e-05,
  max_iter = 500L,
  bootstrap_reps = 0L,
  seed = 1L,
  backend = "auto"
)
}
\arguments{
\item{df}{A panel data.frame (the same one used to fit).}

\item{outcome, group, time, treatment}{Column names.}

\item{method}{One of \code{"fe"}, \code{"ife"}, \code{"mc"}.}

\item{n_placebos}{Integer. Number of pre-treatment placebos to test
per unit. Default \code{3L}. Equivalent to the \code{placebo} option in the
fect package.}

\item{r, lambda, tol, max_iter}{Method-specific args forwarded to the
underlying fit (see \code{\link[=didgpu_fect]{didgpu_fect()}}).}

\item{bootstrap_reps}{Integer. Number of bootstrap replicates for
SE estimation. Default \code{0L} = no bootstrap, SEs are NA.}

\item{seed}{Integer. Bootstrap seed.}

\item{backend}{Backend for the underlying fits.}
}
\value{
A data.frame with one row per placebo horizon: \code{horizon}
(negative, -1 = period immediately before F_g), \code{estimate},
\code{se}, \code{p_value} (test of H0: estimate = 0), plus a \code{joint_p}
attribute giving the joint p-value across all placebos.
}
\description{
For each unit g with first-switch period F_g, "treat" the M
pre-treatment periods immediately before F_g (i.e., periods
F_g - 1, F_g - 2, ..., F_g - M) and refit the fect estimator on
the remaining truly-pre-treatment cells. Effects at those placebo
cells should be statistically indistinguishable from zero if the
identifying assumption holds.
}
\examples{
p <- didgpu_simulate_panel(n_units = 60L, n_periods = 12L,
                            tau_profile = c(0.5, 1.0), seed = 17L)
pl <- didgpu_fect_placebo(p, "Y", "unit", "period", "D",
                           method = "fe", n_placebos = 2L)
pl

}
